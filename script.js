document.addEventListener('DOMContentLoaded', () => {const searchInput = document.getElementById('searchInput');const categoryFilter = document.getElementById('categoryFilter');const imageGallery = document.getElementById('imageGallery');const imageCards = document.querySelectorAll('.image-card');const modal = document.getElementById('imageModal');const modalImage = document.getElementById('modalImage');const modalCaption = document.getElementById('modalCaption');const closeButton = document.querySelector('.close-button');const filterImages = () => {const searchTerm = searchInput.value.toLowerCase();const selectedCategory = categoryFilter.value;imageCards.forEach(card => {const cardName = card.dataset.name.toLowerCase();const cardCategory = card.dataset.category;const cardInfo = card.querySelector('.info p').textContent.toLowerCase();const matchesSearch = cardName.includes(searchTerm) || cardInfo.includes(searchTerm);const matchesCategory = selectedCategory === 'all' || cardCategory === selectedCategory;if (matchesSearch && matchesCategory) {card.style.display = 'flex';} else {card.style.display = 'none';}});};searchInput.addEventListener('input', filterImages);categoryFilter.addEventListener('change', filterImages);imageGallery.addEventListener('click', (event) => {const card = event.target.closest('.image-card');if (card) {const imgSrc = card.querySelector('img').src;const imgAlt = card.querySelector('img').alt;const imgCaption = card.querySelector('.info h3').textContent + ': ' + card.querySelector('.info p').textContent;modal.style.display = 'flex';modalImage.src = imgSrc.replace('300x200', '800x600');modalImage.alt = imgAlt;modalCaption.textContent = imgCaption;}});closeButton.addEventListener('click', () => {modal.style.display = 'none';});modal.addEventListener('click', (event) => {if (event.target === modal) {modal.style.display = 'none';}});document.addEventListener('keydown', (event) => {if (event.key === 'Escape' && modal.style.display === 'flex') {modal.style.display = 'none';}});});